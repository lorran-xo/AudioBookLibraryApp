import colors from './color';
import fonts from './fonts';
import metrics from './metrics';

export const Theme = {
    colors: colors,
    fontSize: fonts.size,
    fontWeight: fonts.weight,
    metrics: metrics,
};
